import { Button } from 'react-bootstrap';

const ConvertButton = () => {
  return (
    <Button variant='primary' size='lg'>
      {' '}
      Convert
    </Button>
  );
};

export default ConvertButton;
